import { redirect } from "next/navigation"; import { Layers3 } from "lucide-react"; import { PlatformMfaSetup } from "@/components/platform/platform-mfa-setup"; import { getPlatformEnrollmentContext } from "@/modules/identity/application/auth";
export const dynamic = "force-dynamic"; export default async function PlatformSetupPage() { const context = await getPlatformEnrollmentContext(); if (!context.ok) redirect("/login?returnTo=/platform/setup"); if (context.value.mfaEnabled) redirect("/platform"); return <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", padding: 24 }}><div style={{ width: "min(620px,100%)" }}><div className="brand" style={{ marginBottom: 20 }}><span className="brand-mark"><Layers3 size={18} /></span>VEYLORIQ Platform</div><PlatformMfaSetup /></div></main> }
